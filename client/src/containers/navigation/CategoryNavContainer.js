import React from 'react';
import CategoryNav from 'components/navigation/CategoryNav';

const CategoryNavContainer = ({ open }) => {
  return <CategoryNav open={open} />;
};

export default CategoryNavContainer;
